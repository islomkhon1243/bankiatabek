const crypto=require('crypto'); const {COOKIE,token,isAuth,json}=require('./_helpers');
exports.handler=async event=>{ if(event.httpMethod==='GET') return isAuth(event)?json(200,{ok:true}):json(401,{error:'Требуется вход'}); if(event.httpMethod==='DELETE') return json(200,{ok:true},{'Set-Cookie':`${COOKIE}=; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=0`}); if(event.httpMethod!=='POST')return json(405,{error:'Метод не поддерживается'}); const password=JSON.parse(event.body||'{}').password||''; const expected=process.env.ADMIN_PASSWORD||''; if(!expected||password.length!==expected.length||!crypto.timingSafeEqual(Buffer.from(password),Buffer.from(expected)))return json(401,{error:'Неверный пароль'}); return json(200,{ok:true},{'Set-Cookie':`${COOKIE}=${token()}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=43200`}); };
